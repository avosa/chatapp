$('#jobs').append("<%= escape_javascript(render 'jobs')%>");
$('#paginator').html("<%= escape_javascript(link_to_next_page(@jobs, 'SEE MORE', remote: true, id: 'load_more'))%>");
if (!$('#load_more').length) { $('#paginator').remove(); }
