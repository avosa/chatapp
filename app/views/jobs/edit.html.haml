.text-center
  = image_tag @job.image.url(:medium)
= render "form"
.text-center.edit-links
  = link_to "Delete Job", job_path(@job), method: :delete, data: { confirm: "Are you sure you want to delete this job?" }
  |
  = link_to "Cancel", jobs_path
